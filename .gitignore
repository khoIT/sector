node_modules/
dist/
build/
.turbo/
coverage/
*.tsbuildinfo
.DS_Store

.env
.env.*
!.env.example

*.log
npm-debug.log*
pnpm-debug.log*

.vite/
.idea/
.vscode/

# Per-agent memory that review tooling writes into the checkout. Globbed from
# any depth: a pattern with an interior slash is anchored to this directory, so
# the unglobbed form missed the copies written under packages/*/.claude/.
**/.claude/agent-memory/

# Netlify CLI local state
.netlify/
