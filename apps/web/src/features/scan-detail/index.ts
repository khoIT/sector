/**
 * The scan detail surface: media viewer, context panel, notes, sharing, and
 * the reviewer's panel for the group and expert queues.
 *
 * Register it by spreading `scanDetailRoutes` into `routes/feature-routes.tsx`.
 * Everything else in this folder is internal to the feature.
 */
export { scanDetailRoutes } from './routes';
