# Written documents, not source. Plans and reports quote code, commit
# messages and the old product name verbatim; reflowing them rewrites the
# record of what was decided and when.
plans/

# Build output and caches.
dist/
build/
.turbo/
coverage/
.vite/

# Generated, and enormous.
pnpm-lock.yaml

# Agent memory, not source.
.claude/agent-memory/

# Half generated from the legacy route map, below the marker; a test asserts
# that half byte for byte. The hand-written half above it is prose, and
# reflowing either would fail the test or rewrite the record.
docs/decommission-dashboard.md
